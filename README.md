# Hemant Bhatta — Portfolio

Simple personal portfolio site.

To preview locally, open `index.html` in your browser or run a static server:

```bash
# Python 3
python -m http.server 8000

# then open http://localhost:8000
```
